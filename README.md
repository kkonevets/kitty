# kitty conf

```sh
sudo add-apt-repository universe
sudo apt install -y fonts-firacode

# чтобы работал по ssh добавь это на целевой машине в .bashrc
echo "TERM=xterm-256color\nforce_color_prompt=yes" >> ~/.bashrc
source ~/.bashrc
```
